import React from "react";

export default function ProtectedRouted({ children, requireAdmin }) {
  // 로그인한 사용자 있는지 확인
  // 그 사용자가 어드민 권한이 있는지 확인
  // requireAdmin이 true인 경우에는 로그인도 되어있어야히ㅏ고 어드민 권한도 가지고 있어야함
}
